# Web App 开发模板

适用于Web应用开发的工作流模板。

## 特点

- 针对前端+后端Web应用优化
- 包含前端开发、后端API、集成测试等阶段
- 适合React、Vue、Next.js等现代Web框架

## 使用

```bash
workflow-bootstrap --template web-app
```

或使用快速启动：

```python
from work_by_roles import Workflow
workflow = Workflow.from_template("web-app")
```

