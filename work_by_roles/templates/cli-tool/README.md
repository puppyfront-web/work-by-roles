# CLI Tool 开发模板

适用于命令行工具开发的工作流模板。

## 特点

- 针对CLI工具开发优化
- 包含命令设计、参数解析、测试等阶段
- 适合Python、Go、Rust等语言的CLI工具

## 使用

```bash
workflow-bootstrap --template cli-tool
```

或使用快速启动：

```python
from work_by_roles import Workflow
workflow = Workflow.from_template("cli-tool")
```

